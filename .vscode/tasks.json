{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build only",
            "type": "shell",
            "command": "docker-compose build --build-arg build=dev",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "isBackground": false,
            "presentation": {
                "reveal": "silent",
                "panel": "dedicated"
            },
            "problemMatcher": "$sampctl"
        }
    ]
}
